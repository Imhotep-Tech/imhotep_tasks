version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: imhotep_tasks_db
      POSTGRES_USER: imhotep_tasks_user
      POSTGRES_PASSWORD: imhotep_tasks_password
    volumes:
      - postgres_data:/var/lib/postgresql/data:Z
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U imhotep_tasks_user -d imhotep_tasks_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    security_opt:
      - seccomp=unconfined
      - label=disable

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/imhotep_tasks/.env
    volumes:
      - ./backend/imhotep_tasks:/app:Z
      - ./backend/imhotep_tasks/old_data:/old_data:Z
    depends_on:
      db:
        condition: service_healthy
    security_opt:
      - seccomp=unconfined
      - label=disable

  frontend:
    build:
      context: ./frontend/imhotep_tasks
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:8000
    volumes:
      - ./frontend/imhotep_tasks:/app:Z
      - /app/node_modules
    depends_on:
      - backend
    security_opt:
      - seccomp=unconfined
      - label=disable

volumes:
  postgres_data:

